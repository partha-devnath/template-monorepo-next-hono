{
  "name": "template-monorepo-next-hono",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "test:watch": "turbo test:watch",
    "test:coverage": "turbo test:coverage",
    "test:e2e": "bun --filter @workspace/db e2e-setup && playwright test --config=e2e/playwright.config.ts",
    "test:e2e:ui": "bun --filter @workspace/db e2e-setup && playwright test --ui --config=e2e/playwright.config.ts",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@eslint/js": "^10",
    "@playwright/test": "1.61.1",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^10",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "turbo": "^2.9.15",
    "typescript": "~7",
    "typescript-eslint": "^8",
    "vitest": "^4.1.10"
  },
  "packageManager": "bun@1.3.5",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
